name: Build 2DFaceID-X

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Theos
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential fakeroot git perl python3 libssl-dev zlib1g-dev
        git clone --recursive https://github.com/theos/theos.git $HOME/theos
        
        # 下载iOS SDK
        mkdir -p $HOME/theos/sdks
        cd $HOME/theos/sdks
        # 使用替代的iOS SDK来源
        # 从官方Theos SDK仓库下载
        wget -q https://github.com/theos/sdks/releases/download/15.5/iOS-15.5.sdk.tar.xz
        tar -xf iOS-15.5.sdk.tar.xz
        rm iOS-15.5.sdk.tar.xz
        # 重命名为iOS.sdk以确保Theos能正确识别
        mv iOS-15.5.sdk iOS.sdk
        
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV
    
    - name: Build 2DFaceID-X
      run: |
        export THEOS=$HOME/theos
        cd $GITHUB_WORKSPACE
        make package FINALPACKAGE=1
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: 2DFaceID-X
        path: "*.deb"
